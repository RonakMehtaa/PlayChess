[phases.setup]
nixPkgs = ["python310", "stockfish"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "chmod +x start.sh"
]

[start]
cmd = "bash start.sh"

[healthcheck]
path = "/health"
